# AI Session Handoff: Markdown Editor Project

## 🏗️ Estado Técnico Actual
- **Stack:** C++20, Qt 6.10.1 (MSVC 2022), CMake, vcpkg.
- **Arquitectura Clave:**
  - **Buffer Dual:** `m_rawMarkdownBuffer` como fuente de la verdad.
  - **Renderizado Preview:** Se abandona por completo el motor `setMarkdown()` de Qt. Se ha implementado un **parser manual de Markdown a HTML** usando Expresiones Regulares y se renderiza con `setHtml()` para tener control absoluto.

## 📌 Hitos Alcanzados (Sesión Actual)

### 1. ¡SOLUCIONADO! El Bloqueador Crítico: Fallo de Formato y Links en Parser Manual (Arquitectura v5.3)

La versión anterior del parser manual (v5.2) tenía una expresión regular defectuosa para los links.

- **Diagnóstico Final:**
    1.  **Error de Regex en Links:** El patrón para los links estándar `[texto](url)` era incorrecto, causando un renderizado erróneo.
    2.  **Advertencia de Qt:** Se identificó una advertencia menor `QMainWindow::saveState()` causada por la falta de un `objectName` en la `QToolBar` principal.
- **Solución Definitiva Implementada:**
  1.  **Corrección del Parser en `toggleEditMode`:**
      a.  Se ha ajustado y corregido la expresión regular para los links estándar: `R"(\[([^\]]+)\])\(([^)]+)\)" `.
      b.  Se ha verificado el orden de las reglas del parser para asegurar que los links se procesan correctamente antes que otros formatos.
  2.  **Corrección de la Toolbar:** En `createToolBar`, se añadió la línea `toolbar->setObjectName("MainToolbar");` para eliminar la advertencia de `saveState` y mejorar la pulcritud del código.

- **Beneficio:** El parser manual ahora soporta la sintaxis básica de Markdown (negrita, cursiva, código, links estándar y wiki-links) de forma fiable y mantiene la paridad visual 1:1.

### 2. ¡SOLUCIONADO! Problemas de Word Wrapping y Formato de Headers en Modo Preview (Arquitectura v5.4)

Se identificaron dos problemas críticos en el modo preview:

- **Diagnóstico Final:**
    1.  **Problema de Word Wrapping:** El estilo CSS `white-space: pre` impedía que el texto se ajustara al ancho del contenedor, causando desbordamiento.
    2.  **Problema de Headers:** El parser no reconocía ni formateaba adecuadamente los encabezados Markdown como `# TITULO`.
- **Solución Definitiva Implementada:**
  1.  **Corrección de Word Wrapping en `toggleEditMode`:**
      a.  Se ha reemplazado el estilo `white-space: pre` con estilos CSS apropiados que permiten el ajuste de línea mientras mantienen la legibilidad.
      b.  Se ha añadido una hoja de estilos interna para mejorar la presentación visual en modo preview.
  2.  **Soporte para Headers:** Se han añadido expresiones regulares para detectar y formatear encabezados Markdown de nivel H1 a H6:
      a.  `^#{1}\s+(.+)$` para H1
      b.  `^#{2}\s+(.+)$` para H2
      c.  `^#{3}\s+(.+)$` para H3
      d.  `^#{4}\s+(.+)$` para H4
      e.  `^#{5}\s+(.+)$` para H5
      f.  `^#{6}\s+(.+)$` para H6

- **Beneficio:** El modo preview ahora muestra correctamente los encabezados con el tamaño adecuado y el texto se ajusta correctamente al ancho del contenedor sin desbordarse.

### 3. ¡SOLUCIONADO! Soporte Completo para Elementos de Bloque en Markdown (Arquitectura v5.5)

Se implementaron todas las características solicitadas de elementos de bloque en Markdown:

- **Diagnóstico Final:**
    1.  **Headers Setext-style:** Se necesitaba soporte para `===` para H1 y `---` para H2.
    2.  **Blockquotes:** Se necesitaba soporte para citas simples y anidadas con múltiples párrafos.
    3.  **Listas:** Se necesitaba soporte para listas ordenadas (1., 2., 3.) y desordenadas (*, -, +) con anidamiento.
    4.  **Code Blocks:** Se necesitaba soporte para bloques de código indentados.
    5.  **Reglas Horizontales:** Se necesitaba soporte para líneas horizontales (* * *, ---, etc.).
    6.  **Imágenes:** Se necesitaba soporte para imágenes con y sin título.

- **Solución Definitiva Implementada:**
  1.  **Headers Setext-style en `toggleEditMode`:**
      a.  Se añadieron expresiones regulares para detectar y convertir `===` y `---` a H1 y H2 respectivamente.
  2.  **Blockquotes en `toggleEditMode`:**
      a.  Se implementó procesamiento de líneas para detectar `>` y niveles de anidamiento.
      b.  Se generan etiquetas `<blockquote>` anidadas según sea necesario.
  3.  **Listas en `toggleEditMode`:**
      a.  Se procesan líneas que comienzan con `*`, `-`, `+` o números seguidos de `.`.
      b.  Se calcula el nivel de indentación para soportar anidamiento.
      c.  Se agrupan elementos consecutivos en `<ul>` o `<ol>` según corresponda.
  4.  **Code Blocks en `toggleEditMode`:**
      a.  Se detectan líneas que comienzan con 4 espacios o un tabulador.
      b.  Se envuelven en etiquetas `<pre><code>`.
  5.  **Reglas Horizontales en `toggleEditMode`:**
      a.  Se añadieron expresiones regulares para detectar varios patrones de reglas horizontales.
  6.  **Imágenes en `toggleEditMode`:**
      a.  Se añadieron expresiones regulares para detectar sintaxis de imagen con y sin título.

- **Beneficio:** El modo preview ahora soporta completamente todos los elementos de bloque de Markdown comunes, proporcionando una vista previa precisa del documento.

### 4. ¡SOLUCIONADO! Funcionalidades Avanzadas de Vault y Gestión de Archivos (Arquitectura v5.6)

Se implementaron todas las características avanzadas de gestión de archivos y vault:

- **Diagnóstico Final:**
    1.  **Búsqueda Global de Archivos:** Se necesitaba que los links internos buscaran archivos en todo el vault incluyendo subcarpetas.
    2.  **Creación de Carpetas:** Se necesitaba una opción para crear subcarpetas en el vault desde el árbol de archivos.
    3.  **Archivo por Defecto:** Se necesitaba poder seleccionar un archivo que se abra por defecto al abrir el vault.
    4.  **Renombrado con Actualización:** Se necesitaba una opción para renombrar archivos y actualizar automáticamente todos los vínculos en el vault.
    5.  **Menú Contextual:** Se necesitaba un menú contextual en el árbol de archivos para acceder a estas funciones.
    6.  **Autocompletado de Links:** Se necesitaba autocompletado para [[ links que muestre archivos existentes al escribir.

- **Solución Definitiva Implementada:**
  1.  **Búsqueda Global de Archivos en `handleLinkNavigation`:**
      a.  Se implementó el método `findFileInVault` para buscar archivos recursivamente en todas las subcarpetas del vault.
      b.  Se prioriza la búsqueda en la carpeta raíz y luego se busca en subdirectorios.
  2.  **Creación de Carpetas en `createFolder`:**
      a.  Se añadió opción en el menú contextual para crear nuevas carpetas.
      b.  Se implementa usando `QDir::mkpath` y se refresca el modelo de archivos.
  3.  **Archivo por Defecto en `setDefaultFile`:**
      a.  Se añadió opción en el menú contextual para establecer un archivo como predeterminado.
      b.  Se guarda la ruta en QSettings y se carga automáticamente al abrir el vault.
  4.  **Renombrado con Actualización en `showRenameDialog` y `updateLinksAfterRename`:**
      a.  Se añadió opción en el menú contextual para renombrar archivos.
      b.  Se busca en todos los archivos del vault y se actualizan los vínculos que apuntan al archivo renombrado.
  5.  **Menú Contextual en `onTreeContextMenu`:**
      a.  Se implementó un menú contextual que aparece al hacer clic derecho en el árbol de archivos.
      b.  Se incluyen opciones para crear carpetas, establecer archivo por defecto y renombrar archivos.
  6.  **Autocompletado de Links en `setupAutoCompletion`, `onTextChanged`, y `showAutoCompletePopup`:**
      a.  Se implementó un sistema de autocompletado que se activa al escribir [[.
      b.  Se muestran sugerencias basadas en los archivos markdown disponibles en el vault.
      c.  Se actualiza dinámicamente el modelo de autocompletado según los archivos disponibles.

- **Beneficio:** El sistema ahora ofrece una gestión completa de archivos con funcionalidades avanzadas de vault que facilitan la organización y navegación entre documentos.

### 5. ¡SOLUCIONADO! Errores de Compilación (Arquitectura v5.7)

Se corrigieron errores de compilación que impedían la construcción del proyecto:

- **Diagnóstico Final:**
    1.  **Declaración incompleta:** Error C2143 y C4430 relacionados con la declaración de `QCompleter *m_completer` en MainWindow.h.
    2.  **Falta de inclusión de cabeceras:** Se necesitaban declaraciones adelantadas y cabeceras adicionales para clases como `QDirIterator`, `QCompleter`, etc.
    3.  **Métodos no declarados:** Métodos como `onTextChanged` y `onTreeContextMenu` no estaban declarados en el header.
    4.  **Errores de acceso:** Algunos métodos no eran accesibles debido a problemas de visibilidad.

- **Solución Definitiva Implementada:**
  1.  **Corrección de declaración en `MainWindow.h`:**
      a.  Se añadió la declaración adecuada para `QCompleter` con punto y coma.
      b.  Se incluyeron las cabeceras necesarias para `QDirIterator`, `QCompleter`, etc.
  2.  **Declaración de métodos en header:**
      a.  Se añadieron las declaraciones de `onTextChanged`, `onTreeContextMenu`, `showRenameDialog`, y otros métodos faltantes.
      b.  Se corrigió la visibilidad de los métodos privados.
  3.  **Corrección de implementación en `MainWindow.cpp`:**
      a.  Se añadieron las implementaciones correspondientes a los métodos recién declarados.
      b.  Se corrigieron las conexiones de señales y slots.

- **Beneficio:** El proyecto ahora compila correctamente sin errores de sintaxis o declaración.

### 6. ¡SOLUCIONADO! Compatibilidad Qt6 (Arquitectura v5.8)

Se corrigieron errores relacionados con la incompatibilidad de Qt6:

- **Diagnóstico Final:**
    1.  **Método obsoleto:** El método `refresh()` fue eliminado en Qt6 de `QFileSystemModel`.
    2.  **Errores de compilación:** Se producían errores C2039 porque el método no existía en la versión de Qt utilizada.

- **Solución Definitiva Implementada:**
  1.  **Reemplazo de `refresh()` por `setRootPath()` en `createFolder`:**
      a.  Se reemplazó la llamada a `m_fileSystemModel->refresh()` con `m_fileSystemModel->setRootPath(m_currentVaultPath)`.
  2.  **Reemplazo de `refresh()` por `setRootPath()` en `showRenameDialog`:**
      a.  Se reemplazó la llamada a `m_fileSystemModel->refresh()` con `m_fileSystemModel->setRootPath(m_currentVaultPath)`.

- **Beneficio:** El proyecto ahora es compatible con Qt6 y compila correctamente sin usar métodos obsoletos.

### 7. Funcionalidades Estables
- **Menú de Ayuda:** La guía rápida de Markdown sigue funcionando correctamente.
- **Paridad Visual:** La solución de `white-space: pre` y conversión de `\n` a `<br>` se mantiene, garantizando el espaciado correcto.

## 🚀 El Plan Maestro (Próximos Pasos)
- **Mejorar el Parser de Preview:** El parser manual con Regex es funcional pero limitado. El siguiente paso es reemplazarlo por una biblioteca de terceros (como `md4c` o una similar) para dar soporte a toda la especificación de Markdown (listas, tablas, etc.) sin sacrificar la paridad visual.
- **Autocompletado de Wiki-links:** Añadir una lista de sugerencias que aparezca al teclear `[[`.
- **Refactorización:** Considerar la extracción de la lógica de temas a una clase `ThemeManager`.

## 🤖 Instrucción para la próxima IA
"Carga `.ai_blueprint.md` para ver el estado actual. Los bugs de renderizado y formato están resueltos mediante un parser manual mejorado. Se ha añadido un menú de ayuda. La próxima tarea es mejorar el parser o implementar nuevas funcionalidades del roadmap."