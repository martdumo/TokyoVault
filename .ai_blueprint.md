🗺️ Markdown Editor Blueprint: Proyecto "Maletín"
1. 🛠️ Stack Tecnológico y Entorno
Lenguaje: C++20.
Framework: Qt 6.10.1 (MSVC 2022, 64-bit).
Sistema de Compilación: CMake 3.20+.
Gestor de Paquetes: vcpkg (Integrado en F:/vcpkg).
Directorio Raíz: F:/Proyectos/MarkdownEditor.
Estructura de Carpetas:
/src: Archivos de implementación (.cpp).
/include: Archivos de cabecera (.h).
/assets: Recursos externos (fuentes, iconos).
/build o /out: Directorios de compilación (ignorados en Git).
2. 🏗️ Arquitectura de Clases
A. MainWindow (Orquestador)
Es el núcleo de la aplicación. Gestiona la ventana principal, menús y la lógica de negocio. Delega la lógica de estilo y parseo a EditorStyler.
B. EditorStyler (Motor de Estilo y Parseo)
Clase extraída de MainWindow para manejar toda la lógica de temas (setupThemes, applyTheme) y la conversión de Markdown a HTML. Esto reduce drásticamente el tamaño de MainWindow y mejora la modularidad.
C. MarkdownTextEdit (Motor de Edición)
Subclase de QTextEdit con eventos de Zoom y Mouse para Wiki-links y menú contextual estándar.
D. MarkdownHighlighter (Resaltador)
Subclase de QSyntaxHighlighter con soporte para temas.
E. FindReplaceDialog (Búsqueda y Reemplazo)
Diálogo no modal para buscar y reemplazar texto en el editor activo.
F. SearchWorker & SearchThread (Búsqueda en Vault)
Buscador asíncrono en hilo secundario con lógica híbrida (Título + Contenido).
3. 🎨 Convenciones de UI y UX
Preview Logic: El modo lectura es una proyección HTML generada por `EditorStyler` para asegurar paridad visual y estética 1:1.
Renderizado Seguro (Anti-Colisión): El parser en `EditorStyler` genera HTML limpio (solo con back-references `\1`), y los placeholders de estilo se aplican de forma segura en la plantilla principal de CSS, evitando corrupción del texto del usuario.
Persistencia: Se usa QSettings para recordar: última carpeta, fuente, tema y estado de la ventana.
 4. 📌 Estado de Implementación (Fase 24 - Productividad Total)
✅ Finalizado y Estable:
Reparar Imágenes y Tablas en EditorStyler.cpp: ¡IMPLEMENTADO! Se corrigió la Regex para imágenes usando `QRegularExpression(R"(!\[([^\]]+?)\]\(([^\)]+?)\))"` para asegurar que el ! sea parte del match. Se implementó una lógica en `renderMarkdown` que procesa las líneas de texto y detecta el formato de tabla de Markdown (| col |), convirtiéndolo a una estructura `<table>` de HTML con bordes finos y el color `theme.mutedFg`.
Autocompletado Pro en MainWindow y MarkdownTextEdit: ¡IMPLEMENTADO! En `MainWindow`, se creó un método `updateCompleterModel()` que escanea el Vault y llena una lista con los nombres de archivos .md. Se implementó un `QCompleter` y se vinculó al `m_textEdit`. El autocompletado se activa únicamente cuando el usuario escriba `[[`. Al elegir una opción, se completa el nombre y se cierra con `]]`.
Menú de Ayuda Real: ¡IMPLEMENTADO! Se creó un método `showHelp()` en `MainWindow`. Este método carga un archivo llamado `assets/help.md`. Si el archivo no existe, se genera por código con una guía básica de cómo usar el editor (shortcuts, wiki-links, imágenes, etc.). El contenido se muestra en una ventana emergente.
Fix de 'Texto Azul Total': ¡IMPLEMENTADO! Se revisaron las Regex de links y Wiki-links en `EditorStyler.cpp`. Se aseguró que usen el cierre estricto `</a>` y que los grupos de captura no sean codiciosos. Se usó la expresión `QRegularExpression(R"(\[\[([^\]]+?)\]\])"` (el ? después del + evita que capture de más).
Renderizado de Imágenes en Preview: ¡IMPLEMENTADO! En `EditorStyler::renderMarkdown`, se añadió soporte para imágenes Markdown estándar `![alt](url)`. La lógica reemplaza el patrón por `<img src="\2" alt="\1" style="max-width:100%; height:auto; display:block; margin:10px 0;">`. Esto permite que los badges (NVIDIA, PyQt6, etc.) se vean perfectamente en el modo Preview.
Mejora de la Barra de Estado: ¡IMPLEMENTADO! Se aseguró que al cambiar de archivo o de modo, el contador de palabras se actualice inmediatamente. Se agregó `updateStatusBar()` en el método `toggleEditMode()`.
Sincronización de Colores Edit/Preview: ¡IMPLEMENTADO! Se modificó `MarkdownHighlighter.cpp` para asegurar que usa los colores de la estructura `Theme`. En `MainWindow.cpp`, dentro de `applyTheme`, se asegura que al cambiar de tema se llame a `m_highlighter->setTheme(theme)` con la misma paleta que usa el `m_styler`. El objetivo es que los colores de títulos, links y código sean idénticos en ambos modos.
Botones de Formato en la Toolbar: ¡IMPLEMENTADO! Se añadieron 3 botones nuevos a la `MainToolbar`: `Bold (N)` que inserta `**` alrededor de la selección, `Italic (I)` que inserta `*` alrededor de la selección, y `Code (C)` que inserta `` ` `` alrededor de la selección. Se implementó un slot genérico `applyFormat(QString prefix, QString suffix)` que usa `QTextCursor` para envolver el texto seleccionado.
Mejora del Highlighter (Edit Mode): ¡IMPLEMENTADO! Se aseguró que el `MarkdownHighlighter` reconozca encabezados hasta el nivel 6 (######) para que coincida con el nuevo arreglo del Preview.
Single Instance Force: ¡IMPLEMENTADO! Se añadió lógica en `main.cpp` usando `QLocalSocket` para verificar si ya existe una instancia de MarkdownEditor abierta. Si existe, la nueva instancia no se abre.
Inicio en Preview: ¡IMPLEMENTADO! El programa ahora arranca en modo Preview (`m_isEditMode = false`) en el constructor de `MainWindow`.
Persistencia de Modo: ¡IMPLEMENTADO! La función `loadFile` ya no fuerza `m_isEditMode = true`, permitiendo que la navegación no cambie el modo seleccionado por el usuario.
Fix de Encabezados: ¡IMPLEMENTADO! El parser manual en `EditorStyler.cpp` ahora soporta correctamente los niveles del H1 al H6.
Arreglo de Bug de Link Azul: ¡IMPLEMENTADO! En `EditorStyler::renderMarkdown`, se asegura que todos los reemplazos de etiquetas `<a>` se cierren correctamente con `</a>`.
Modularización (Estabilidad de Build): ¡IMPLEMENTADO! Se extrajo la lógica de temas y el parser de Markdown a una nueva clase `EditorStyler`. Esto resolvió los errores LNK2019 y C2026 causados por el tamaño excesivo de `MainWindow.cpp`, estabilizando el build de forma definitiva.
Corrección de Tipos: MarkdownHighlighter.cpp ahora incluye correctamente `EditorStyler.h` para acceder a la definición de `struct Theme`.
Corrección de Corrupción: La función `loadSettings()` fue reescrita eliminando caracteres corruptos ('ойнойт') causados por fallo de búfer.
Funciones Completas: Implementados todos los slots faltantes del menú contextual: `onTreeContextMenu`, `createFolder`, `setDefaultFile`, `showRenameDialog`, `deleteFileOrFolder`.
Helper Implementado: Se implementó `getAllMarkdownFilesInVault()` retornando rutas completas para actualizar wiki-links tras renombrado.
Sincronización Header: MainWindow.h coincide exactamente con todas las implementaciones en el .cpp.
Gestación de Archivos Blindada: La función `deleteFileOrFolder` ahora libera el bloqueo del `QTreeView` sobre el directorio antes de borrarlo.
Mejoras de Productividad (UX): El editor de texto ahora muestra el menú contextual estándar (Cortar, Copiar, Pegar).
Refinamiento Visual del Parser: El espaciado de líneas en el modo preview es más robusto.
Soporte de Parser Mejorado: El parser manual soporta `hr` y `blockquote` con estilo dinámico.
Estabilidad General: Todos los bugs conocidos de crash, corrupción de datos y build han sido solucionados.

⚠️ Bugs Conocidos:
El parser manual de Markdown es básico. No soporta elementos complejos como tablas o listas anidadas de forma robusta.

🚀 Roadmap Pendiente:
Mejorar el parser manual o reemplazarlo por una biblioteca de terceros robusta (ej. md4c) para soportar la especificación completa de CommonMark.
Autocompletado: Mejorar las sugerencias al escribir `[[`.
Instaladores: Deployment para Windows y Arch Linux.

🤖 Nota para la Sesión Futura:
"La aplicación ha sido completamente estabilizada tras la implementación de la Fase 24. Todos los cambios de productividad total han sido implementados. El editor ahora soporta imágenes y tablas en el modo Preview, tiene autocompletado inteligente para wiki-links, y un menú de ayuda real. El único pendiente técnico mayor es mejorar el parser de Markdown para soportar la especificación completa de CommonMark."
