🗺️ Markdown Editor Blueprint: Proyecto "Maletín"
1. 🛠️ Stack Tecnológico y Entorno
Lenguaje: C++20.
Framework: Qt 6.10.1 (MSVC 2022, 64-bit).
Sistema de Compilación: CMake 3.20+.
Gestor de Paquetes: vcpkg (Integrado en F:/vcpkg).
Directorio Raíz: F:/Proyectos/MarkdownEditor.
Estructura de Carpetas:
/src: Archivos de implementación (.cpp).
/include: Archivos de cabecera (.h).
/assets: Recursos externos (fuentes, iconos).
/build o /out: Directorios de compilación (ignorados en Git).
2. 🏗️ Arquitectura de Clases
A. MainWindow (Orquestador)
Es el núcleo de la aplicación. Gestiona la ventana principal, menús y la lógica de negocio.
Manejo de Vault: Usa QFileSystemModel + QSortFilterProxyModel para navegar por carpetas y archivos .md.
Sistema de Buffer Dual: Mantiene m_rawMarkdownBuffer como la "única fuente de la verdad" del texto Markdown.
Motor de Temas: Múltiples temas dinámicos con sincronización entre editor y preview.
B. MarkdownTextEdit (Motor de Edición)
Subclase de QTextEdit con eventos de Zoom y Mouse para Wiki-links.
C. MarkdownHighlighter (Resaltador)
Subclase de QSyntaxHighlighter con soporte para temas.
D. FindReplaceDialog (Búsqueda y Reemplazo)
Diálogo no modal para buscar y reemplazar texto en el editor activo.
E. SearchWorker & SearchThread (Búsqueda en Vault)
Buscador asíncrono en hilo secundario con lógica híbrida (Título + Contenido).
3. 🎨 Convenciones de UI y UX
Filtro de Vault: El buscador mantiene el rootIndex en la carpeta del Vault activo.
Preview Logic: El modo lectura es una proyección HTML generada manualmente. Se abandona setMarkdown() y se usa un parser manual (Regex) + setHtml() para asegurar paridad visual y estética 1:1.
Renderizado Seguro (Anti-Colisión): La lógica de reemplazo del parser en `toggleEditMode` ahora genera HTML limpio (solo con back-references `\1`), y los placeholders de estilo (`%1`, `%2`, etc.) se aplican de forma segura al final en la plantilla principal de CSS, evitando corrupción del texto del usuario.
Persistencia: Se usa QSettings para recordar: última carpeta, fuente, tema y estado de la ventana.
4. 📌 Estado de Implementación (Fase 19.0)
✅ Finalizado y Estable:
Gestión de Archivos Robusta: ¡SOLUCIONADO! La función `deleteFileOrFolder` ahora elimina carpetas de forma recursiva usando `QDir::removeRecursively()`, corrigiendo el fallo anterior que solo eliminaba archivos.
Estabilidad del Parser (Anti-Colisión): ¡SOLUCIONADO! Se corrigió la colisión de placeholders en el parser del modo Preview. La lógica de reemplazo ahora es segura y no corrompe el texto del usuario que pueda contener `%1`, etc.
Estabilidad de Build: Corregido el error de sintaxis C2059 en el parser manual. El build es estable.
Paridad Estética Total (Editor/Preview): Implementada la sincronización dinámica de colores y fuentes.
Estabilidad Crítica (Crash): Corregido el crash de Access Violation en el menú contextual del TreeView.
Paridad Visual (Saltos de Línea): La regresión de líneas vacías ha sido reparada permanentemente con `white-space: pre-wrap`.
Funcionalidad de Buscar y Reemplazar: Implementada y funcional.
Portabilidad de Build (Linux): `CMakeLists.txt` es ahora portable.

⚠️ Bugs Conocidos:
El parser manual de Markdown es básico. No soporta elementos complejos como tablas o listas anidadas de forma robusta.

🚀 Roadmap Pendiente:
Mejorar el parser manual o reemplazarlo por una biblioteca de terceros robusta (ej. md4c) para soportar la especificación completa de CommonMark.
Autocompletado: Mejorar las sugerencias al escribir `[[`.
Instaladores: Deployment para Windows y Arch Linux.
Refactorización: Considerar la extracción de la lógica de temas a una clase `ThemeManager`.

🤖 Nota para la Sesión Futura:
"La aplicación es estable. Se han corregido errores críticos de build, corrupción de datos en el preview y bugs de funcionalidad como el borrado de carpetas. El siguiente paso debe ser la mejora del parser de Markdown para soportar sintaxis avanzada (tablas, etc.)."